the hyperparameters are for the pictures under 'test' and 'train' files, 
if you run other pictures such as two_objects.png the result is not that good.

In hysteresis function，I used union-set to label point rather than the method on slides, the efficiency is almost the same.
